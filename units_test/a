#!/usr/bin/env bash

# Description of the unit
description() {
    echo "Description a"
}

# Dependencies for the unit (space-separated list)
dependencies() {
    echo "d"
}

# Main logic of the unit
main() {
    echo "running a"
}

# Ensure main function runs if the script is called directly
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main
fi
